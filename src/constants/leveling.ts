// Lowered from 500 — at the 5-challenge daily cap (~250-300 XP/day), 500 meant over
// two full days per level, which read as barely any progress. 200 gets a level roughly
// every 3-4 challenges, so a level-up shows up within a single day of normal play.
export const XP_PER_LEVEL = 200;
